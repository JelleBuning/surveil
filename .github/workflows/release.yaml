name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  test:
    uses: ./.github/workflows/build-and-test.yaml

  release:
    needs: test
    runs-on: windows-latest
    strategy:
      matrix:
        platform: [x86, x64, ARM64]
        include:
          - platform: x86
            rid: win-x86
          - platform: x64
            rid: win-x64
          - platform: ARM64
            rid: win-arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Extract version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $displayVersion = $tag.TrimStart('v')
          $manifestVersion = $displayVersion + '.0'
          "DISPLAY_VERSION=$displayVersion" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "VERSION=$manifestVersion" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Update Package.appxmanifest version
        shell: pwsh
        run: |
          $manifest = "src\Surveil\Package.appxmanifest"
          $version = "${{ steps.version.outputs.VERSION }}"
          (Get-Content $manifest -Raw) -replace 'Version="\d+\.\d+\.\d+\.\d+"', "Version=`"$version`"" |
            Set-Content $manifest -NoNewline

      - name: Setup .NET environment
        uses: ./.github/actions/setup-dotnet-env

      - name: Restore dependencies
        run: dotnet restore

      - name: Build and Package MSIX
        shell: pwsh
        run: |
          dotnet publish src\Surveil\Surveil.csproj `
            -c Release `
            -r ${{ matrix.rid }} `
            -p:Platform=${{ matrix.platform }} `
            -p:GenerateAppxPackageOnBuild=true `
            -p:AppxPackageDir="${{ github.workspace }}\AppxPackages\" `
            -p:AppxPackageSigningEnabled=false `
            -p:AppxBundle=Never

      - name: Rename MSIX with platform suffix
        id: rename
        shell: pwsh
        run: |
          $package = Get-ChildItem -Path "${{ github.workspace }}\AppxPackages" -Filter "*.msix" -Recurse |
            Select-Object -First 1
          $newName = Join-Path $package.DirectoryName "Surveil_${{ steps.version.outputs.DISPLAY_VERSION }}_${{ matrix.platform }}.msix"
          Rename-Item -Path $package.FullName -NewName $newName
          "MSIX_PATH=$newName" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: AppxPackages/**/*.msix